# requirements.txt
#
# Minimal dependencies needed for Phase 1 backend structure
# (app factory + config + extensions only — no models/routes logic yet).

Flask
Flask-SQLAlchemy
Flask-CORS
python-dotenv
PyMySQL
